name: Build Windows App

on:
  push:
    branches:
      - main
  workflow_dispatch:
    inputs:
      skip_bundle:
        description: '快速模式：只產生 EXE，跳過安裝包'
        required: false
        default: false
        type: boolean

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '24'
          cache: 'npm'

      - name: Install Rust (with Windows SDK)
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Setup MSVC Developer Command Prompt
        uses: ilammy/msvc-dev-cmd@v1

      - name: Rust Cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: "src-tauri -> target"

      - name: Install Frontend Dependencies
        run: npm ci

      - name: Build Tauri EXE (快速模式)
        if: ${{ github.event.inputs.skip_bundle == 'true' }}
        run: npm run tauri build -- --no-bundle

      - name: Build Tauri App (完整模式 — 含 NSIS/MSI 安裝包)
        if: ${{ github.event.inputs.skip_bundle != 'true' }}
        run: npm run tauri build

      # === Portable EXE ===
      - name: Upload Portable EXE
        uses: actions/upload-artifact@v4
        with:
          name: WisdomBoard-Portable
          path: src-tauri/target/release/wisdomboard.exe

      # === NSIS 安裝包 ===
      - name: Upload NSIS Installer
        if: ${{ github.event.inputs.skip_bundle != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: WisdomBoard-NSIS-Installer
          path: src-tauri/target/release/bundle/nsis/*.exe

      # === MSI 安裝包 ===
      - name: Upload MSI Installer
        if: ${{ github.event.inputs.skip_bundle != 'true' }}
        uses: actions/upload-artifact@v4
        with:
          name: WisdomBoard-MSI-Installer
          path: src-tauri/target/release/bundle/msi/*.msi
